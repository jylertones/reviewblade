import { style } from "@vanilla-extract/css";

export const checkRunsList = style({
  display: "flex",
  alignItems: "center",
  gap: "0.5rem",
  listStyle: "none",
  padding: 0,
  marginBlock: 0,
});
